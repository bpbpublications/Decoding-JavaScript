import { add } from './utils.js';

// We import the function add here by specifying the name and mentioning the path from where we import it.

let result = add(5, 6);
document.getElementById('result').innerHTML = result;
